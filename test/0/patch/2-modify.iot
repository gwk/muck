{
  'desc': 'modify file.',
  'compile': [
    # for file systems with poor timestamp resolution, e.g. HFS+, a `sleep 1` is necessary for consistent testing.
    "sh -c 'echo modified text. > _build/patched.txt'",
    ],
  'cmd': '$MUCK patched.txt',
}
