{
  'desc': 'modify file.',
  'compile': [
    # sleep necessary due to poor HFS+ timestamp resolution.
    "sh -c 'sleep 1 && echo modified text. > _build/patched.txt'",
    ],
  'cmd': '$MUCK patched.txt',
}
